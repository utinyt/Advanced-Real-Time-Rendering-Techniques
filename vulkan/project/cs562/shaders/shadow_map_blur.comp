#version 460 core

layout(local_size_x = 128, local_size_y = 1, local_size_z = 1) in;

layout(binding = 0, rgba32f) uniform readonly image2D src;
layout(binding = 1, rgba32f) uniform writeonly image2D dst;
layout(binding = 2) uniform readonly BlurKernelBuffer {
	float kernel[101];
};

void main(){
	imageStore(dst, ivec2(gl_GlobalInvocationID.xy), vec4(1, 0, 0, 1));
}
